% Compatibility wrapper for the active plane-source benchmark.
% Keep this file only so older notes or shortcuts still resolve.

run(fullfile(fileparts(mfilename('fullpath')), 'm1_plane_source_benchmark.m'));
